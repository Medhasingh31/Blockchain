{
  "name": "blockchain-project",
  "version": "1.0.0",
  "description": "Hardhat blockchain project for Polygon Mumbai",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:feedback": "hardhat run scripts/deploy-feedback.js --network amoy",
    "deploy:feedback:local": "hardhat run scripts/deploy-feedback.js --network hardhat",
    "server": "node server.js",
    "test:api": "node test-api.js",
    "test:ipfs": "node test-ipfs.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "express": "^4.18.3",
    "form-data": "^4.0.0"
  }
}
